/**
 * Il motore di composizione PDF è generico (foglio parametrico, non solo A4 verticale) e
 * serve anche a "dichiarazioni", non solo al fascicolo apparecchiatura: vive in
 * `services/pdfCompose/`. Questo file resta come punto d'importazione storico.
 */
export * from '@/services/pdfCompose/componiPdf'
